[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "frontend-e2e-tests"
version = "1.0.0"
dependencies = [
    "selenium",
    "webdriver-manager"
]